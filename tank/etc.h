#include <iostream>
#include <graphics.h>

class etc
{
     private :
       void *img;
     public:
       void animation(int,int,int);
       void Draw_Die(int,int); 
       void Draw_Born(int,int);
};
